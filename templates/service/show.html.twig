{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Serv_nom</th>
                <td>{{ service.servNom }}</td>
            </tr>
            <tr>
                <th>Serv_telephone</th>
                <td>{{ service.servTelephone }}</td>
            </tr>
            <tr>
                <th>Serv_mail</th>
                <td>{{ service.servMail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_index') }}">back to list</a>

    <a href="{{ path('app_service_edit', {'id': service.id}) }}">edit</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
